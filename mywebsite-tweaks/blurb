Keeps www.petergrecian.co.uk healthy and improving — the keeper that knows the site deeply and takes work on it, including from other forkterms.

The mywebsite-keeper: it understands `www.petergrecian.co.uk` (repo `~/mywebsite` — Lambda + API Gateway + Cloudflare + DynamoDB) thoroughly and improves it when asked, including work requests handed over from other forkterms, not just directly from Peter. The site in one line: Peter's personal showcase — CV at `/`, plus `/contents` nav, `/gardencam*`, `/pi-fleet`, `/t3`, `/lambda-stats`, `/glacier`, and the astro/storage pages. Deploys with `./deploy` (auto-purges Cloudflare cache); infra via terraform; this strand dir holds only curation. Don't confuse with [[cv]] (separate repo, w3.petergrecian.co.uk).
