ding --arm waiters die instantly in some sessions (seen 2026-08-20, astro-polecam). Four arms, all killed within ~1-2 min; task output file EMPTY (killed by signal, not ding's own exit 3). Pinning DING_OWNER=<claude pid> did NOT help — waiter verified alive at 11s with DING_OWNER set, then killed. Meanwhile astro-science (armed 12:53) and hardware (16:15) waiters with IDENTICAL command lines ran for hours in the same ps. So: not a ding bug and not owner-death self-clean — something in that session's background-task handling reaps them. Worth finding, because the Stop hook then nags every turn and the session silently loses its doorbell (fallback: strand-mailbox peek at checkpoints).
