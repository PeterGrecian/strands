IDENTITY DIVERGENCE BUG (seen live 2026-08-09): the deck ran a term whose @strand tag said 'astro-storage' but the ACTUAL process on that tty was 'aicli home-automation' (claude --add-dir .../home-automation). The tag and the running session had silently drifted — for the whole session everyone (incl. the conductor) believed astro-storage was backfilling when it was really home-automation. '@strand as clobber-proof truth' only holds if it's kept in sync on swap/relaunch; here a relaunch reused a pane without re-tagging. FIX DIRECTION: the deck should VERIFY @strand against the running process (e.g. read the tty's aicli/claude --add-dir arg, or CLD_STRAND env) rather than trust the tag; a periodic reconcile that flags/repairs drift. Also astro-storage turned out NOT to be running anywhere despite being 'on the deck' all session.
