pane VERB TOOL built+working 2026-08-09 (poc/pane): list/up/drop/grow/even/ribbon/restore, all deck-aware (keeps PANE_KEEPER registry consistent), reuses pane-conductor-helpers.sh. Proven live: ribboned+restored ubersitrep, registry stayed clean, [ribbon] mark works. NO bug — the width-shrink worked as told (earlier note wrongly called it a geometry bug; the thin sliver was because Peter had manually reflowed the deck to read the chat, not a tool defect). OPEN DESIGN QUESTION (not a bug): what geometry should a ribbon actually have? Peter's mental model = a small strip PARKED UNDER the top-left strands term (a relocated short tile). tmux's binary-split-tree can't MOVE a term to an arbitrary position by resize alone — needs join-pane/select-layout. So ribbon-PLACEMENT is its own design pass; current impl just shrinks width in place. Also: help sed grabs a few lines past usage (cosmetic). Next: promote 2-3 verbs to bind -n root chords once the vocabulary settles.
