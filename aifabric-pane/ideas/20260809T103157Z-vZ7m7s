CLARIFIED (supersedes earlier confusion) 2026-08-09: the BORDER is the reliable strand identity (tmux, deck-controlled, e.g. '3: ubersitrep'). Claude's STATUS LINE is app plumbing and mostly NOISE for glancing — after /model it repainted to 2 lines: 'ubersitrep · opus 4.8' + '⏵⏵ bypass permissions on · 1 shell · 1 agent'. Only the strand token is useful and it duplicates the border. Peter: '1 shell 1 agent not useful info'. Direction: TRUST the border for identity; QUIETEN Claude's status line for keepers (minimal/blank statusLine via aicli launch config — per-session Claude setting, NOT a tmux/deck lever). Don't try to make the status line the identity source.
