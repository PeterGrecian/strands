DESIGN FORK (competes with 'border=truth' note) — Peter 2026-08-09: may DROP borders for GRAPHICAL reasons (clean tiled look, coloured tiles JOIN UP = Windows-8/Metro, the earlier 'tiles join up' wish; borders prevent that). If borders go, the STATUS LINE must become the identity layer instead. Spec: ONE line (not two), content = strand-name · context-size · git-status — drop the plumbing (1 shell/1 agent/bypass). Build = custom Claude Code statusLine script keepers launch with (via aicli), NOT a tmux lever. TRADEOFF: identity then depends on the app-inside rendering — during a /model overlay etc. identity VANISHES (border never had this since it's outside the app). Decide: clean look vs. overlay-proof identity. Ties to: quieten-status-line note, tiles-join-up/Metro, aicli launch config.
