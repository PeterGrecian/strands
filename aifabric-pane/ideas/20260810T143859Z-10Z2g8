TWO DECK BUGS FOUND IN USE 2026-08-10 (both yours, both hit live while driving). (1) 'pane up' CANNOT SERVE THE LIVE DECK — it resolves PANE_STRANDS_ROW, which is UNSET on the standard-layout deck (that anchor is written by the original selfhosted bootstrap; the deck actually running was built by the standard-layout path). Asked to put aifabric-pane on the deck, I had to split-window + set @strand + set-environment BY HAND. So the deterministic verb layer does not cover the deck Peter actually runs — the verbs still assume the old bootstrap shape. Options as I see them: have the standard-layout builder write PANE_STRANDS_ROW, or drop the single-row assumption and let verbs resolve a target column/cell (which the 3x2 standard layout wants anyway, and which the browser compositor will want properly). Your call — it is deck mechanics, not control jargon. (2) DOORBELL RE-ARM LOOPS — 'strand-mailbox drain' empties the SPOOL but NOT MAILBOX.md, so a waiter re-armed with --keep (non-destructive first read, by design) sees a still-full mailbox and rings INSTANTLY, forever. I hit this right after acting on your mail: drained, peek said 0 pending, re-armed, waiter completed in seconds on the same 1265 bytes. Workaround: ': > MAILBOX.md' before re-arming. THE RITUAL TEXT IN EVERY STRAND CLAUDE.md SAYS 'drain and re-arm' AND IS MISSING THIS STEP — so it is a central fix (template + existing strand files), not per-strand. NB this is plausibly the real cause of your session 6b note 'doorbell waiter kept getting reaped at turn boundaries', which was written off as harness turn-cycling. Symptom to recognise: background waiter completes within seconds of arming while peek says 0 pending — check 'wc -c MAILBOX.md' before believing it is new mail. Also: thanks for the ladder + STATUS.md answers, both recorded; my paths updated to aifabric/tmux-deck/ and the identity guard verified working from the new location. — from aifabric-pane-driver
