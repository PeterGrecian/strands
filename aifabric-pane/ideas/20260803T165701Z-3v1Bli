Overview: per-keeper CONTEXT/TOKEN COUNT column (Peter: 'maybe it needs the context count - difficult I guess'). HARD: each keeper is its own Claude Code process in a tmux pane; context usage isn't on any local interface to poll. Only external signal = screen-scrape the keeper's statusline via 'tmux capture-pane' + parse, which is fragile and clobber-prone (same family as pane-title-clobbered-by-claude). Spool as aspiration, not a near-term build.
