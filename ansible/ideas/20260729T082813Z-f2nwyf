NFS-export bigstore 'bs' to the fleet (Peter, 2026-07-29): add an ansible role/task to NFS-export the bigstore disk bs from its host and mount it on the astro-writing host(s). Static transport layer; ansible already owns fleet exports+mounts, so just another export/mount pair. Load-bearing dependency: astro-storage's end-of-night sync writes onto this mount — so this lands first, then the sync graduates in astro-storage. Decide: which host exports, which host(s) mount, mount point, export options (async/sync, squash).
