Consider a dedicated tether host for the EOS if muppet's sessions-index ingest
goes continuous.

Today the fussy gphoto2 drive path hung reliably when muppet load spiked — root
cause was a stuck-gphoto2 pileup (self-inflicted), not OpenSearch (which idles
at ~3% CPU). BUT the aifabric-sessions ingest is currently HOURLY; if it goes
CONTINUOUS it would compete with the camera's timing-sensitive USB path more
often. If that becomes a real problem, move the tethered EOS off muppet to a
dedicated host.

Candidate: vole (Acer C720) — but it's currently PARKED mid-flash to MrChromebox
UEFI, half-dead screen, 2GB RAM, and is the aifabric x86 tiebreaker node; NOT a
ready headless tether host. Moving the tether is a real project: install
gphoto2, replicate the udev autosuspend rule (04a9:32e1 power/control=on) + mask
gvfs-gphoto2-volume-monitor, port the whole tether recipe from STATE.md. A Pi
could also serve. Not a tonight action; revisit only if muppet load actually
starts biting the drive path.
