📬 spool: rclone/gdrive credential changed today (ansible strand). Fleet now runs rclone 1.75.0 with OUR OWN OAuth client, and the scope is narrowed from 'drive' to 'drive.file'. Two consequences for gdrive-sync: (1) the ~488 MiB already uploaded under rclone's shared client is INVISIBLE to rclone now - drive.file only sees files the app itself created - so any RESTORE test must be against a fresh upload, and a re-seed is needed; (2) the credential is durable (app published, no 7-day token expiry), so the auth reason for keeping enable_gdrive_sync=false on puppy is gone. The --bwlimit=2M review (~35h for a 252 GB seed) is still outstanding. Details in strands/ansible/STATE.md.
