# super — state

*Curated summary of where this strand is. Updated at the end of each session.*

## What exists

- (new strand — nothing recorded yet)

## Pending / loose ends

- **Topology for a phone-reachable strand team** (promoted 2026-07-23; partly
  done). Plan: run `strandchat` on **puppy** — co-locates the mesh tree +
  resident strandterms + the web window (inotify waiters need the mailbox fs
  local), with `tailscale serve` fronting HTTPS, which also makes the OSD
  dashboard :5601 phone-reachable for free. **Puppy is now on the tailnet**
  (joined ~2026-07-22), so the prerequisite is met — remaining work is
  standing up strandchat there + the serve config. Notes: homepi is
  windows-only (ample but agents can't live there); vole stays lean
  (tiebreaker duty); muppet joins the tailnet only if the team outgrows puppy.
  **Caution:** resident strandterms drink the 5h quota window — arm-and-idle
  discipline required.

## Decisions
