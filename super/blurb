The super meta-repo, carved down — daily-kitchen tooling and conventions, with the AI method migrating out to aifabric.

The `~/super` meta-repo: house tools (`secrets`, `resolve-host`, `alert`, `trash`, …), the global context (`GLOBAL.md`), and cross-repo conventions. The *content* of strands lives in the `strands` repo; super owns the *tooling and conventions* around them. (Note: aicli/strand tooling moved to `aifabric` — super no longer symlinks into it.)
